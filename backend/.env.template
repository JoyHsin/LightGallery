# LightGallery Backend Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database host (production)
DB_HOST=your-production-db-host.com

# Database port
DB_PORT=3306

# Database name
DB_NAME=lightgallery

# Database username
DB_USERNAME=lightgallery_app

# Database password (use a strong password)
DB_PASSWORD=your-strong-database-password

# =============================================================================
# JWT CONFIGURATION
# =============================================================================

# JWT signing secret (CRITICAL: Must be at least 256 bits)
# Generate with: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-at-least-256-bits-long

# =============================================================================
# OAUTH CONFIGURATION - WECHAT
# =============================================================================

# WeChat App ID (from WeChat Open Platform)
WECHAT_APP_ID=your-wechat-app-id

# WeChat App Secret (from WeChat Open Platform)
WECHAT_APP_SECRET=your-wechat-app-secret

# =============================================================================
# OAUTH CONFIGURATION - ALIPAY
# =============================================================================

# Alipay App ID (from Alipay Open Platform)
ALIPAY_APP_ID=your-alipay-app-id

# Alipay Private Key (RSA2048)
ALIPAY_PRIVATE_KEY=your-alipay-private-key

# Alipay Public Key (from Alipay)
ALIPAY_PUBLIC_KEY=your-alipay-public-key

# =============================================================================
# OAUTH CONFIGURATION - APPLE
# =============================================================================

# Apple Client ID (Bundle Identifier)
APPLE_CLIENT_ID=joyhisn.LightGallery

# Apple Team ID (from Apple Developer Account)
APPLE_TEAM_ID=your-apple-team-id

# Apple Key ID (from Apple Developer Account)
APPLE_KEY_ID=your-apple-key-id

# Apple Private Key (from Apple Developer Account)
APPLE_PRIVATE_KEY=your-apple-private-key

# =============================================================================
# APPLE IAP CONFIGURATION
# =============================================================================

# Apple IAP Shared Secret (from App Store Connect)
APPLE_IAP_SHARED_SECRET=your-apple-iap-shared-secret

# Apple IAP Environment (sandbox or production)
APPLE_IAP_ENVIRONMENT=production

# =============================================================================
# SSL/TLS CONFIGURATION
# =============================================================================

# Path to SSL keystore file
SSL_KEY_STORE=/etc/lightgallery/keystore.p12

# SSL keystore password
SSL_KEY_STORE_PASSWORD=your-keystore-password

# SSL key alias
SSL_KEY_ALIAS=lightgallery

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=https://lightgallery.app,https://www.lightgallery.app

# =============================================================================
# NOTES
# =============================================================================

# 1. Generate JWT Secret:
#    openssl rand -base64 64

# 2. Generate Strong Password:
#    openssl rand -base64 32

# 3. Convert SSL Certificate to PKCS12:
#    openssl pkcs12 -export \
#      -in fullchain.pem \
#      -inkey privkey.pem \
#      -out keystore.p12 \
#      -name lightgallery

# 4. Verify Environment Variables:
#    source .env && env | grep -E "DB_|JWT_|WECHAT_|ALIPAY_|APPLE_"

# 5. Security:
#    - Never commit this file with actual values
#    - Use strong, unique passwords
#    - Rotate secrets regularly (every 90 days)
#    - Limit access to this file (chmod 600)
